apiVersion: v1
kind: Service
metadata:
  name: python-fastapi
  namespace: getwebstack
  labels:
    app: python-fastapi
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: python-fastapi
